return {
  {
    "MeanderingProgrammer/render-markdown.nvim",
    opts = {
      -- This prevents the current line from "popping" back to raw text
      anti_conceal = {
        enabled = false,
      },
    },
  },
}
